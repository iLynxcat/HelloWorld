inherent filesize test (experiment)
	a "hello world" in different systems languages,
	just to see the binary's inherent filesize.

make clean all:
	remove all built binaries, then build and run each.
